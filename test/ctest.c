#include <stdio.h>
#include <complex.h>
#include <math.h>

int main()
{
    // double c = 1.0;
    // double a = pow(0.1, 17);
    // double b = 2.0*pow(0.1, 17); 
    // double a = 1.0;
    // double b = 1.0;  

    // double complex a;
    // a = cexp(3/16*2*M_PI*I);

    // double a = -2*M_PI/8;    
    // double m = 4.0;
    // double x = a*m;
    // printf("%.20lf\n", cimag(cexp(x*I)));

    // if (fabs(x)>=M_PI){
    //     x = x > 0 ? (x - M_PI) : (x + M_PI);
    // }

    // // printf("%.20lf", sin(M_PI)*pow(10,16));
    // printf("%.20lf", cimag(cexp(x*I))*pow(10,16));

    printf("%.20lf\n", -1.0);
    // printf("%.20lf\n%.20lf\n%.20lf\n%.20lf", (a*pow(10,17)-c)*pow(10,17), b*pow(10,17), (a/2-b)*pow(10,17), a/2);

    return 0;
}